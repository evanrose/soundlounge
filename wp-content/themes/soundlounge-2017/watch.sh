# This is the command that watches for changes and builds .css files.
# You can run this file on the command line by making it executable (only once) chmod +x watch
# and then starting it by typing ./watch
bundle exec compass watch
